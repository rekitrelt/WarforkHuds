if %RACE == 0
if %HEALTH != #NOTSET

	//background
	setAlign #CENTER, #MIDDLE
	setcursor #WIDTH / 2, 150 + #HEIGHT / 2
	setColor 1, 1, 1, 1
	setSize 10 * 1.333 * %VIDHEIGHT / %VIDWIDTH, 10
	drawNum %SPEED
endif
endif

